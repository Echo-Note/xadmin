# Docker 远程主机配置
# 使用方法: 复制此文件为 .env 并修改为你自己的 Docker 服务器地址
#   cp .env.example .env
# 如果使用本地 Docker，可删除 .env 文件或注释掉以下配置

# Docker 守护进程地址
# TCP 格式: tcp://<host>:<port>        例: tcp://172.16.30.120:2375
# SSH 格式: ssh://<user>@<host>         例: ssh://user@172.16.30.120
# 本地 Docker: 删除此行或留空
DOCKER_HOST=tcp://172.16.30.120:2375
