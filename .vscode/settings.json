{
  // VS Code 工作区设置
  // 指定后端 Python 解释器为 server/.venv
  "python.defaultInterpreterPath": "${workspaceFolder}/server/.venv/bin/python",
  "python.analysis.extraPaths": [
    "${workspaceFolder}/server"
  ]
}
